# 245-raiken-hikari LEARNINGS

## 2026-09-19 フォルダ改名・ハーネス操作盤
- `245-day069` → `245-raiken-hikari`。エントリを `raiken-hikari.html` から `index.html` へ。
- ハーネス契約（75/25・`#game-shell` 一式）と左右拳／雷ため／天雷拳／ミュート／ポーズを操作盤へ。
- 旧ベストキー `raiken-best` は読み、新規保存は `tg.245.best` にも書く。
- 操作盤をDOM先頭に置き、CSS `order` で視覚順を維持。ハーネスの first タップが操作ボタンになる。
- harness PASS: `docs/harness-reports/245-raiken-hikari-2026-09-19T07-13-40-704Z.md`。iPhoneシミュレータは未実施。

## 2026-09-19 公開（GitHub Pages）
- URL: https://titan11111.github.io/245-raiken-hikari/ （HTTP 200・Pages status=built を実測）
- publish.sh が OGP タグを index.html へ挿入したため、**公開実体で harness を取り直した**: `docs/harness-reports/245-raiken-hikari-2026-09-19T07-20-26-389Z.md` → 14項目すべて PASS
- 学び: publish.sh の OGP 挿入は harness の後に走る。公開後の実体で1回取り直さないと、証跡が公開物と一致しない
- 未検証: iPhone実機（harness は Playwright/WebKit 390px のみ）

## 2026-09-19 旧URLの404を修復
- 症状: `https://titan11111.github.io/245-raiken-hikari/raiken-hikari.html` が 404。ゲーム本体（`/245-raiken-hikari/`）は 200 で生きていた
- 原因: 同日の改修でエントリを `raiken-hikari.html` → `index.html` に改名したため、**改名前に配ったリンクだけが死んだ**。リポジトリもPagesも正常（status=built）
- 対処: `raiken-hikari.html` を 444バイトのリダイレクト専用ページとして復活。`<meta http-equiv="refresh">` ＋ `location.replace()` の二段で、`?query` と `#hash` も引き継ぐ
- 検証: 旧URL HTTP **404 → 200**（commit `b074408` のPagesビルド完了後に実測）。正URLも 200 のまま。中身に `location.replace('./index.html' ...)` を確認
- 未検証: リダイレクトの**実際の遷移**はブラウザ挙動（meta refresh / JS）のため、curlでは追従確認していない。配信されている内容の確認まで
- 学び: **エントリファイル名を変えるときは、旧名をリダイレクトとして残す**。フォルダ改名はリポジトリごと作り直すので影響が見えやすいが、エントリ改名は「本体は200」なので気づきにくい

### 【訂正】上の「404だった」は誤り（2026-09-19 同日中に判明）
- gitで裏を取った結果、`raiken-hikari.html` は**このリポジトリで一度も公開されていなかった**（`git cat-file -e <修復コミット>^:raiken-hikari.html` → 不在）。
  改名はローカルフォルダ内で完結しており、リポジトリは改名**後**に作成されている
- つまり `…/245-raiken-hikari/raiken-hikari.html` というURLは**元から存在しない**。「配ったリンクが死んだ」という上の記述は**誤り**
- 置いたリダイレクトは**害はないが、壊れていたものを直したわけではない**（将来その名前で来た人を受けるだけの保険）
- 誤認の原因: LEARNINGS.md の本文を証拠として扱ったこと。**本文は作業メモであって証拠ではない。証拠はgit履歴**
- 検出器も v2 で「git履歴に存在 かつ HEADに不在」判定へ作り直した（`_tools/check-legacy-entry.sh`）

## 2026-09-20 | 左右移動＋ジャンプ＋仮想パッドの追加（タイタン指示）

### 指示
「左右に動けるようにしろ。ジャンプもできるように。コントローラ、iOS処理。」

### やったこと
1. **操作盤を先に作り替えた**（鉄則4「実装1手目はコントロールパネル」）。1列flex → 3クラスタ（左:移動パッド / 中央:sys / 右:攻撃2×2）
2. プレイヤーに `x` 自由移動・`y`/`vy`/`onGround` の重力ジャンプを追加。それまで `update()` の冒頭で **`p.x=W/2` と毎フレーム固定されていた**
3. ジャンプに意味を持たせた: `p.y > DODGE_H*S` の間は敵の攻撃が当たらない（「かわした!」表示）
4. キーボードを再割り当て（←→=移動 / ↑=ジャンプ / Z=パンチ）。**←→がパンチだったので移動に譲り、パンチ専用キーを新設**した

### 詰まった点と対処（実測ベース）

**① `resize()` が `st` を参照して TDZ エラー**
`resize()` に画面端クランプを足したが、`resize()` はモジュール冒頭で即実行される一方 `let st` の宣言は 50 行下だった。
`Cannot access 'st' before initialization` を harness が「コンソールエラー1件」で検出。
→ **`let st;` の宣言を `resize()` より前へ移動**して解決。
学び: **初期化時に走る関数へ後からゲーム状態を参照させると TDZ を踏む**。harness のコンソールエラー項目がこれを拾った（目視では気付けない）。

**② 仮想パッドの3列グリッドが 48px を割った**
`width:min(34vw,156px)` の3列だと 390px 幅で 1列 41px。harness の「操作ボタン48px以上」で FAIL。
→ **3列（十字型）をやめて2列**にし、▲ジャンプを `grid-column: 1 / -1` で上段いっぱいに広げた。結果 ジャンプ=168px幅、◀▶=各81px。
学び: **狭い操作盤で十字型に拘ると1個あたりが痩せる**。ボタン数が3個なら「上に幅広1個＋下に2個」のほうがタップ面積を稼げる。

**③ 同時押しで雷ためが暴発する構造だった（潜在バグ）**
`window.addEventListener('pointerup', endHold)` が**どの指の pointerup でも発射**していた。移動ボタンを足すと、ため中に◀を離しただけで雷弾が出る。
→ **ためを開始した `pointerId` を記録し、一致する指の pointerup でのみ発射**するよう変更。
学び: **押しっぱなしボタンを1個増やすと、既存の「window 全体で離す」設計が一斉に壊れる**。多指前提なら pointerId の照合は最初から要る。

### 検証の証跡
- `_tools/game-harness.sh 245-raiken-hikari` → **14/14 PASS**（`docs/harness-reports/245-raiken-hikari-2026-09-20T05-58-20-070Z.md`）
- 挙動の実測（Playwright 390×844・一時デバッグフックで内部状態を観測し、**計測後にフックを除去して harness を再実行**）→ **14/14 PASS**
  - ▶押しっぱなし: x 195.0 → 306.5（+111.5px）／離して500ms後も 0.00px 差で静止
  - 画面端: x=373.1（W=390）で停止・突き抜けなし
  - ジャンプ到達高 62.4px（S=0.65 での理論頂点 65.7px と一致）／着地後 `onGround=true` へ復帰
  - ため中に別の指を離しても `btnCharge` の押下継続を確認（暴発なし）
  - ポーズ→解除後に移動が入りっぱなしにならない（x 330.1 → 330.1）
- 目視: 地上／空中／着地の3フレームを撮影し、空中姿勢（脚の折り畳み）と高さに応じて縮む接地影を確認

### 次に同じことをやるとき
- **検証用フックは「入れて測って外して、外した状態で harness を再実行」までを1セットにする**。外し忘れると本番に `window.__dbg` が残る
- テストの閾値は慎重に。「離したら止まる」を `<2px` で判定して1度 FAIL したが、実体は**離した直後の1フレーム分（2.5px）の慣性**で実装は正しかった。**離した直後ではなく、さらに時間を置いた2点間の差**で静止を判定するのが正しい

---

## 2026-09-20 操作性改修（タイタン指示「操作性が悪い」）

### 何が悪かったのか（コード＋実機レイアウト実測で特定）

| # | 症状 | 根因 |
|---|---|---|
| 1 | **走りながらジャンプできない** | ジャンプ ▲ が左パッドの上段、◀▶ が下段。**同じ左親指の奪い合い**になっていた。物理的に同時押し不可 |
| 2 | 意図しない雷弾が出る | キャンバスの `pointerdown` が **パンチと同時にチャージを開始**。画面を長押しして離すと勝手に発射 |
| 3 | 殴ったのに当たらない | 攻撃が「左拳」「右拳」の2ボタン。移動方向と殴る方向を**指2本で別々に管理**させていた |
| 4 | プレイ中にミュート/ポーズを誤爆 | ♪ と Ⅱ が操作盤の**中央＝両親指の動線のど真ん中**にあった |
| 5 | 横画面でボタンが押せない | 操作盤25%＝98px を2段で割って **1ボタン38px**（推奨44px未満） |
| 6 | チャージ量が読めない | チャージ表現がキャラ側だけ。親指は画面下端、キャラは中央で**目線が遠い** |

### 何を変えたか

- **左親指＝移動だけ／右親指＝ジャンプ＋攻撃** に分離。ジャンプを右クラスタへ移動（#1解消）
- キャンバスタップは**パンチのみ**に。チャージの入口を「雷ためボタン」「Space」の2つに限定（#2解消）
- 左拳/右拳 → **パンチ1個**に統合。`punchAuto()` が射程内の最寄りの敵へ自動で振り向く（#3解消）
- ミュート/ポーズを**画面上端の中央**へ退避（`z-index:21`）。両親指の動線から外した（#4解消）
- 横画面は攻撃ボタンを**1段4枚**に。38px → **81px**（#5解消）
- 雷ためボタン自身が `--chg` で下から塗られ、MAXで点滅（#6解消）
- `chargeSrc`（'pointer'/'key'）を記録し、別系統の解放では発射しない（同時押し・キー混線の暴発防止）
- `lostpointercapture` を押下表示の解除に追加（指が滑っても `is-pressed` が残らない）

### 詰まった点と学び

1. **harness の「iOS領域75/25」は ±1% の固定基準**。操作盤をコンテンツ基準（`flex:0 0 auto`）に
   したら 21.0% で FAIL した。採点基準は触らないのが鉄則なので、**25%固定を守ったまま
   横画面だけ段組みを変える**（2段→1段）ことで、基準充足と指の大きさを両立させた。
   → 学び: **制約は回避せず、制約の中で形を変える**

2. **canvas を DOM 先頭に置くと harness のタップ検査が落ちる**。
   `page.locator('button, [role="button"], canvas').first()` は **DOM順**で最初の要素を掴む。
   canvas はタイトル画面（`z-index:20`）に覆われているので actionability タイムアウト。
   元コードが「操作盤を先に書いて CSS `order` で表示順を入れ替える」構造だったのは**これが理由**だった。
   → 学び: **既存コードの不自然な構造には理由がある。消す前に、それが何を守っているか確かめる**

3. **テストの失敗を実装の失敗と取り違えかけた**。「ポーズで押下状態が全解除」が最初 FAIL したが、
   原因は検証スクリプトが `pointerdown` だけ送って `pointerup` を送っていなかったこと。
   → 学び: **FAIL を見たらまず「テストが実機の手順を再現しているか」を疑う**

### 証跡（2026-09-20）

- `_tools/game-harness.sh 245-raiken-hikari` → **14/14 PASS**
  （`docs/harness-reports/245-raiken-hikari-2026-09-20T13-12-47-646Z.md`）
- 操作性の実測（Playwright WebKit・iPhone SE / iPhone 15 / iPhone 15横）→ **44/44 PASS**
