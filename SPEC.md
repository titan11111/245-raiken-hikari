# 雷拳ヒカリ — 仕様

## 0. ドキュメント情報
- 対象: 245-raiken-hikari / 更新日: 2026-09-20 / ステータス: 公開

## 1. ゲーム概要
浮遊都市の屋上を左右に走り、ジャンプで敵の攻撃をかわしながら、パンチ・雷弾・天雷拳でWAVEを生き残りスコアを伸ばすアクション。

## 2. 対象環境
GitHub Pages / iPhone Safari / 静的HTML。外部CDNなし。

## 3. 操作 / 設定UI

### 操作盤の構成（下25% `#control-deck`・3クラスタ）
| 位置 | 中身 |
|---|---|
| 左 `.pad` | ▲ジャンプ（上段・横いっぱい）／◀ ▶ 移動（下段） |
| 中央 `.sys` | ♪ ミュート ／ Ⅱ ポーズ |
| 右 `.acts` | 左拳・右拳・雷ため・天雷拳（2×2） |

### 入力の対応表
| 操作 | タッチ | キーボード |
|---|---|---|
| 左右移動（押しっぱなし） | ◀ ▶ | ← → / A D |
| ジャンプ | ▲ | ↑ / W |
| パンチ | 左拳・右拳ボタン／キャンバスタップ（タップ側へ） | Z / J / Enter |
| 雷ため→雷弾 | 雷ため長押し→離す | Space 長押し→離す |
| 天雷拳 | 天雷拳（ゲージMAXで有効） | K |
| ポーズ | Ⅱ | P / Escape / タブ非表示 |

### 入力の実装規約
- Pointer Events に統一。全ボタンで `setPointerCapture`。移動ボタンは押しっぱなし用に `bindHold()`（`pointerup` / `pointercancel` / `lostpointercapture` の3系統で解除）
- **雷ためは開始したポインタIDを記録**し、別の指の `pointerup` では発射しない（移動ボタンとの同時押しでの暴発防止）
- ポーズ・ゲームオーバー・`blur`・リセット時に押下状態を全クリア（`clearHeld()`）
- 操作ボタンは `touch-action: none`、body は `manipulation`

### 移動・ジャンプの物理（すべて画面スケール `S` 倍）
| 定数 | 値 | 意味 |
|---|---|---|
| `MOVE_SPD` | 245 px/s | 走行速度 |
| `JUMP_V` | 620 px/s | 初速（滞空 約0.65秒・頂点 約101×S px） |
| `GRAV` | 1900 px/s² | 重力 |
| `AIR_CTRL` | 0.82 | 空中の左右制御の効き |
| `EDGE` | 26 px | 画面端のクランプ幅 |
| `DODGE_H` | 52 px | **この高さより上なら敵の攻撃をかわせる**（ジャンプの存在理由） |

- 雷弾は撃った高さから発生し、地上高（`groundY-68×S`）へ滑り降りる。当たり判定は水平のみなので空中撃ちでも当たる
- パンチの当たり判定は高さを問わない（空中でも当たる）

### 保存キー
- ミュート: `tg.245.mute` ／ ベスト: `tg.245.best`（旧 `raiken-best` を読み替え・両方へ書き込み）

## 4. 検証済み（2026-09-20）
- `_tools/game-harness.sh 245-raiken-hikari` → **14/14 PASS**（レポート: `docs/harness-reports/245-raiken-hikari-2026-09-20T05-58-20-070Z.md`）
- 挙動の実測（Playwright・一時フックで内部状態を観測、計測後フックは除去）→ **14/14 PASS**
  移動量・離したら静止・向き・画面端クランプ・ジャンプ到達高・着地復帰・キーボード←↑・ジャンプ回避・同時押し暴発なし・ポーズ解除後の入力残りなし・JSエラー0

## 5. 未確定事項
なし（公開ブロッカーなし）
