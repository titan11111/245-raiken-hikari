# 雷拳ヒカリ — 仕様

## 0. ドキュメント情報
- 対象: 245-raiken-hikari / 更新日: 2026-09-20（操作性改修） / ステータス: 公開

## 1. ゲーム概要
浮遊都市の屋上を左右に走り、ジャンプで敵の攻撃をかわしながら、パンチ・雷弾・天雷拳でWAVEを生き残りスコアを伸ばすアクション。

## 2. 対象環境
GitHub Pages / iPhone Safari / 静的HTML。外部CDNなし。

## 3. 操作 / 設定UI

### 設計の原則（2026-09-20 操作性改修で確定）
**左親指＝移動だけ／右親指＝ジャンプと攻撃**。旧版はジャンプが左パッドの上段にあり、
◀▶ と同じ親指を奪い合うため「走りながら跳ぶ」が物理的に不可能だった。これを右手側へ移した。

### 操作盤の構成（下25% `#control-deck`・2クラスタ）
| 位置 | 中身 |
|---|---|
| 左 `.pad` | ◀ ▶ 移動（縦いっぱいの2枚。押しっぱなし） |
| 右 `.acts` | 縦画面は2×2（上段: 天雷拳・ジャンプ／下段: 雷ため・パンチ）。**横画面は1段4枚**（操作盤25%＝約98pxで2段だと1枚43pxになり指が入らないため） |

システム系（♪ミュート・Ⅱポーズ）は操作盤から外し、**画面上端の中央**へ置く。
両親指の動線から外れるので、プレイ中の誤爆が起きない。`z-index:21` でタイトル/KO画面より前面。

### 入力の対応表
| 操作 | タッチ | キーボード |
|---|---|---|
| 左右移動（押しっぱなし） | ◀ ▶ | ← → / A D |
| ジャンプ | ジャンプ（右下クラスタ） | ↑ / W |
| パンチ | パンチ（**射程内の最寄りの敵へ自動で振り向く**）／キャンバスタップ（タップ側へ） | Z / J / Enter |
| 雷ため→雷弾 | 雷ため長押し→離す | Space 長押し→離す |
| 天雷拳 | 天雷拳（ゲージMAXで点滅） | K |
| ポーズ | 画面上端の Ⅱ | P / Escape / タブ非表示 |

### 入力の実装規約
- Pointer Events に統一。全ボタンで `setPointerCapture`。押下表示の解除は
  `pointerup` / `pointercancel` / **`lostpointercapture`** の3系統（指が滑っても残らない）
- **キャンバスタップはパンチのみ**。旧版はタップで同時にチャージが始まり、長押しして指を離すと
  意図しない雷弾が出ていた。チャージの入口は「雷ためボタン」と「Spaceキー」の2つだけにした
- **チャージの発生源を `chargeSrc`（'pointer' / 'key'）で記録**し、別系統の解放では発射しない
  （移動ボタンとの同時押し／キーとタッチの混線での暴発防止）
- **雷ためボタン自身がチャージ量を表示する**（`--chg` で下から塗り、MAXで点滅）。
  チャージ表現がキャラ側にしかないと、親指の位置から目線が遠くて量が読めない
- 天雷拳は撃てるときだけ `.ready` で点灯・脈動。撃てないときは `disabled`
- ポーズ・ゲームオーバー・`blur`・リセット時に押下状態とチャージを全クリア（`clearHeld()`）
- 操作ボタンは `touch-action: none`、body は `manipulation`
- **DOM順は 操作盤 → ステージ**、表示順は CSS `order` で入れ替える。
  canvas を DOM 先頭に置くと harness のタップ検査が canvas を掴み、タイトル画面に覆われて FAIL する

### 移動・ジャンプの物理（すべて画面スケール `S` 倍）
| 定数 | 値 | 意味 |
|---|---|---|
| `MOVE_SPD` | 245 px/s | 走行速度 |
| `JUMP_V` | 620 px/s | 初速（滞空 約0.65秒・頂点 約101×S px） |
| `GRAV` | 1900 px/s² | 重力 |
| `AIR_CTRL` | 0.82 | 空中の左右制御の効き |
| `EDGE` | 26 px | 画面端のクランプ幅 |
| `DODGE_H` | 52 px | **この高さより上なら敵の攻撃をかわせる**（ジャンプの存在理由） |

- 雷弾は撃った高さから発生し、地上高（`groundY-68×S`）へ滑り降りる。当たり判定は水平のみなので空中撃ちでも当たる
- パンチの当たり判定は高さを問わない（空中でも当たる）

### 保存キー
- ミュート: `tg.245.mute` ／ ベスト: `tg.245.best`（旧 `raiken-best` を読み替え・両方へ書き込み）

## 4. 検証済み（2026-09-20 操作性改修後）
- `_tools/game-harness.sh 245-raiken-hikari` → **14/14 PASS**
  （レポート: `docs/harness-reports/245-raiken-hikari-2026-09-20T13-12-47-646Z.md`）
- 操作性の実測（Playwright WebKit・iPhone SE / iPhone 15 / iPhone 15横）→ **44/44 PASS**
  - 全8ボタンが3端末すべてで 44px 以上（横画面の攻撃ボタンは 38px → **81px**）
  - 移動クラスタと攻撃クラスタが左右に完全分離＝両親指の同時押しが成立
  - **走りながらジャンプ**: ▶ を押したままジャンプしても ▶ の押下が維持される
  - **キャンバス長押しでチャージしない**（`--chg` が 0 のまま）＝雷弾の暴発なし
  - 雷ため: ボタンにチャージ量が出る／MAXで点滅／離すとリセット
  - 別の指を離しても雷ためが解除されない（同時押し暴発なし）
  - ポーズで押下状態が全解除／JSエラー 0件

## 5. 未確定事項
なし（公開ブロッカーなし）
